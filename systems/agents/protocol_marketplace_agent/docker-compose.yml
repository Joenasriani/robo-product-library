services:
  protocol-marketplace-agent:
    build: .
    container_name: protocol-marketplace-agent
    restart: unless-stopped
    ports:
      - "${PORT:-8009}:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
