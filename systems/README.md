# Systems

This folder contains runtime systems and internal software components.

## Structure

- `agents/` — operational agent applications and marketplace backends
- `rag/` — non-product RAG system applications and reference implementations

These are not automatically treated as sellable SKUs unless explicitly listed in `/products/catalog.yaml`.
