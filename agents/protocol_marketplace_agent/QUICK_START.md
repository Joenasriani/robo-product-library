# Protocol Marketplace Agent - Quick Start

```bash
cp .env.example .env
# Edit .env: set ADMIN_TOKEN
docker-compose up -d
curl http://localhost:8009/health
```

Browse marketplace: http://localhost:8009
