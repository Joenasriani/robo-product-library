services:
  sales-outreach-agent:
    build: .
    container_name: sales-outreach-agent
    restart: unless-stopped
    ports:
      - "${PORT:-8001}:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
