services:
  social-content-ops-agent:
    build: .
    container_name: social-content-ops-agent
    restart: unless-stopped
    ports:
      - "${PORT:-8004}:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
