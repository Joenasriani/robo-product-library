services:
  lead-research-agent:
    build: .
    container_name: lead-research-agent
    restart: unless-stopped
    ports:
      - "${PORT:-8002}:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
