# QA Checklist

1. Validate all YAML files in `core/` parse successfully.
2. Verify conservative fallback behavior on low-confidence intent.
3. Verify escalation handoff routes to a human operator.
4. Confirm privacy and safety constraints from `docs/limitations.md` are enforced.
5. Confirm acceptance criteria in `tests/acceptance_criteria.yaml` are recorded in test evidence.
